<h1>Carrinho do Cliente <%= @cart.customer.name %></h1>
<h3>Itens</h3>
<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Quantidade</th>
      <th>Preço</th>
    </tr>
  </thead>
  <tbody>
    <% @cart.cart_items.each do |item| %>
      <tr>
        <td><%= item.item.name %></td>
        <td><%= item.quantity %></td>
        <td><%= number_to_currency(item.item.price) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Editar', edit_cart_path(@cart), class: 'btn btn-warning' %>
<%= link_to 'Voltar', carts_path, class: 'btn btn-secondary' %>