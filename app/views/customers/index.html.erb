<h1>Clientes</h1>
<%= link_to 'Novo Cliente', new_customer_path, class: 'btn btn-primary mb-3' %>
<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th>Endereço</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @customers.each do |customer| %>
      <tr>
        <td><%= customer.name %></td>
        <td><%= customer.email %></td>
        <td><%= customer.address %></td>
        <td><%= link_to 'Mostrar', customer, class: 'btn btn-secondary' %></td>
        <td><%= link_to 'Editar', edit_customer_path(customer), class: 'btn btn-warning' %></td>
        <td><%= link_to 'Excluir', customer, method: :delete, data: { confirm: 'Você tem certeza?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>