<h1><%= @item.name %></h1>
<p><%= @item.description %></p>
<p>Preço: <%= number_to_currency(@item.price) %></p>
<p>Quantidade em Estoque: <%= @item.stock_quantity %></p>
<%= link_to 'Editar', edit_item_path(@item), class: 'btn btn-warning' %>
<%= link_to 'Voltar', items_path, class: 'btn btn-secondary' %>